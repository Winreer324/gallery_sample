name: gallery_for_lena
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  json_serializable: ^4.1.2
  dio: ^4.0.0
  cached_network_image: ^3.0.0
dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_bloc: 7.0.0
  build_runner: ^2.0.4
flutter:

  uses-material-design: true

